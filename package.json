{
  "name": "@TinycellCorp/cheatjs",
  "version": "2.0.10",
  "description": "Game engine agnostic cheat UI (bottom sheet)",
  "main": "cheat.js",
  "browser": "cheat.min.js",
  "types": "cheat.d.ts",
  "unpkg": "cheat.min.js",
  "jsdelivr": "cheat.min.js",
  "sideEffects": true,
  "files": [
    "cheat.js",
    "cheat.min.js",
    "cheat.d.ts"
  ],
  "scripts": {
    "inject-version": "node scripts/inject-version.js",
    "minify": "terser cheat.js -o cheat.min.js -c -m",
    "type-check": "tsc --noEmit cheat.d.ts",
    "validate-types": "node scripts/validate-types.js",
    "build": "npm run inject-version && npm run minify && npm run validate-types && npm run type-check",
    "prepublishOnly": "npm run build",
    "version": "npm run build && git add cheat.js cheat.min.js",
    "release:patch": "npm version patch -m \"release: v%s\" && git push && git push --tags && npm publish && npm run deploy:preview",
    "release:minor": "npm version minor -m \"release: v%s\" && git push && git push --tags && npm publish && npm run deploy:preview",
    "release:major": "npm version major -m \"release: v%s\" && git push && git push --tags && npm publish && npm run deploy:preview",
    "copy-to": "npm run build && node scripts/copy-to.js",
    "dev": "vite dev --config dev/vite.config.js",
    "build:preview": "npm run build && vite build --config dev/vite.config.js",
    "deploy:preview": "npm run build:preview && gh-pages -d dist-preview"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "terser": "^5.37.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/TinycellCorp/cheatjs.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "license": "MIT"
}
